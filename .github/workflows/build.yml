name: Build

on:
  push:
  pull_request:

jobs:
  universal-macos-app:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7
      - name: Build and verify universal app
        run: make verify
      - name: Package
        run: make package
      - uses: actions/upload-artifact@v7
        with:
          name: OpenGuard-universal
          path: dist/OpenGuard-1.1.0-universal.zip
