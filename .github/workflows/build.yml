name: Build

on:
  push:
  pull_request:

jobs:
  universal-macos-app:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7
      - name: Build and verify universal app
        run: make verify
